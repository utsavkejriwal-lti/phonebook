export class Contact{
    firstName: string;
    lastName: string;
    contNum: string;

    constructor(first, last, cont){
        this.firstName = first;
        this.lastName = last;
        this.contNum = cont;
    }
}